[pytest]
addopts = -q --strict-markers --maxfail=1
testpaths = tests
asyncio_mode = auto
markers =
  gpu: требует GPU
  slow: долгие тесты
  redis: требует Redis
  db: требует Postgres
  e2e: сквозные
  asyncio: async тесты
