[pytest]
addopts = -q --strict-markers --maxfail=1
testpaths = tests
asyncio_mode = auto
markers =
  gpu: Requires GPU
  slow: Slow tests
  redis: Requires Redis
  db: Requires Postgres
  e2e: End-to-end tests
  asyncio: Async tests
