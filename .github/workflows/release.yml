name: release

on:
  push:
    tags:
      - "v*"

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  version-check:
    name: version-check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Verify tag matches project version
        shell: python
        run: |
          import tomllib
          from pathlib import Path

          pyproject = tomllib.loads(Path("pyproject.toml").read_text(encoding="utf-8"))
          project_version = pyproject["project"]["version"]
          tag = "${{ github.ref_name }}"
          expected = f"v{project_version}"
          if tag != expected:
              raise SystemExit(f"Tag {tag!r} does not match project version {expected!r}")

  frontend:
    name: frontend
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: version-check
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci

      - name: Typecheck frontend
        run: npm run typecheck --if-present || npx tsc --noEmit

      - name: Lint frontend
        run: npm run lint --if-present

      - name: Build frontend
        run: npm run build

      - name: Upload frontend artifact
        uses: actions/upload-artifact@v7
        with:
          name: frontend-dist
          path: frontend/dist
          if-no-files-found: error

  python-lint:
    name: python-lint
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: version-check
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.11"
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Install dependencies
        run: uv sync --extra dev --frozen

      - name: Ruff check
        run: uv run ruff check app tests

      - name: Ruff format check
        run: uv run ruff format --check app tests scripts migrations

  python-typecheck:
    name: python-typecheck
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: version-check
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.11"
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Install dependencies
        run: uv sync --extra dev --frozen

      - name: MyPy typecheck
        run: uv run mypy app

  backend-fast:
    name: backend-fast
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: version-check
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.11"
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Install dependencies
        run: uv sync --extra dev --frozen

      - name: Backend fast unit tests
        env:
          SECRET_KEY: test-secret-key-for-release
          LIMITS_ENABLED: "true"
          NO_REDIS: "true"
          DEBUG: "true"
          REFRESH_COOKIE_SECURE: "false"
        run: |
          uv run pytest -m "not (gpu or redis or db or e2e or slow or perf)" --maxfail=1 --disable-warnings -q --no-cov

  backend-integration:
    name: backend-integration
    runs-on: ubuntu-latest
    timeout-minutes: 35
    needs: version-check
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: testdb
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
      redis:
        image: redis:7
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.11"
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Install dependencies
        run: uv sync --extra dev --frozen

      - name: Run backend integration suite
        env:
          DATABASE_URL: postgresql+psycopg2://postgres:postgres@localhost:5432/testdb
          REDIS_URL: redis://localhost:6379/0
          SECRET_KEY: test-secret-key-for-release
          LIMITS_ENABLED: "true"
          NO_REDIS: "false"
          DEBUG: "true"
          REFRESH_COOKIE_SECURE: "false"
        run: |
          uv run pytest tests --cov=app --cov-report=term-missing --cov-report=xml --cov-fail-under=60 --junitxml=pytest.xml

      - name: Upload release backend artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: release-backend-artifacts
          path: |
            coverage.xml
            pytest.xml
          if-no-files-found: warn

  create-release:
    name: create-release
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs:
      - version-check
      - frontend
      - python-lint
      - python-typecheck
      - backend-fast
      - backend-integration
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Download frontend artifact
        uses: actions/download-artifact@v8
        with:
          name: frontend-dist
          path: release-assets/frontend-dist

      - name: Create GitHub release
        uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true
