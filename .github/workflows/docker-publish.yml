name: docker-publish

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]

env:
  IMAGE_BACKEND: ${{ secrets.REGISTRY }}/genai-backend
  REGISTRY_HOST: ${{ secrets.REGISTRY_HOST }} # e.g. ghcr.io or docker.io

jobs:
  build-push-backend:
    name: backend image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY_HOST }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_TOKEN }}

      - name: Compute tags
        id: vars
        run: |
          SHA=${GITHUB_SHA::7}
          BR=${GITHUB_REF_NAME}
          echo "sha_tag=sha-$SHA" >> $GITHUB_OUTPUT
          echo "branch_tag=branch-$BR" >> $GITHUB_OUTPUT
          if [ "$BR" = "main" ]; then
            echo "extra=latest,staging" >> $GITHUB_OUTPUT
          else
            echo "extra=" >> $GITHUB_OUTPUT
          fi

      - name: Build & Push (backend)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_BACKEND }}:${{ steps.vars.outputs.sha_tag }}
            ${{ env.IMAGE_BACKEND }}:${{ steps.vars.outputs.branch_tag }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Trivy scan (backend image)
        uses: aquasecurity/trivy-action@0.24.0
        with:
          image-ref: ${{ env.IMAGE_BACKEND }}:${{ steps.vars.outputs.sha_tag }}
          format: 'table'
          exit-code: '1'
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'
